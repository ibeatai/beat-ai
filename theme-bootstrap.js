// AUTO-GENERATED by scripts/build-theme-bootstrap.mjs — do not edit.
// Default theme + mode come from src/components/themeSelector/config.js.
// Runs before the React bundle: migrates the legacy 'theme' key and sets
// data-theme(-mode) up front so first-time visitors don't flash the wrong theme.
(function () {
  var DEFAULT_THEME_ID = 'classic-blue';
  var DEFAULT_THEME_MODE = 'light';

  // Migrate the old 'theme' (dark|light) value into 'theme-mode'.
  var oldTheme = localStorage.getItem('theme');
  if (oldTheme === 'dark' || oldTheme === 'light') {
    localStorage.setItem('theme-mode', oldTheme);
    localStorage.removeItem('theme');
  }

  var themeMode = localStorage.getItem('theme-mode') || DEFAULT_THEME_MODE;
  var gradientTheme = localStorage.getItem('docs-theme') || DEFAULT_THEME_ID;

  document.documentElement.setAttribute('data-theme-mode', themeMode);
  document.documentElement.setAttribute('data-theme', gradientTheme);
})();
